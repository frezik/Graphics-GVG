Version 0.4
===========
* Include files
* Arc
* Rotate ellipses
* Bezier curve
* Chipmunk renderer (as its own module)
* SVG renderer (as its own module)
* Callback system that will unify walking the AST across different renderers
* Inkscape extension
    * Reading
    * Writing
